import React from 'react'

export const metadata = {
  title: 'Video Directory',
  description: 'A video directory application built with Payload CMS and Next.js',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 